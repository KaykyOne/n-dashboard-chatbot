export enum WhatsAppProvider {
  BAILEYS = "BAILEYS",
  EVOLUTION = "EVOLUTION",
}

export enum InstanceStatus {
  ONLINE = "ONLINE",
  OFFLINE = "OFFLINE",
  CONNECTING = "CONNECTING",
}

export enum Autor {
  USUARIO = "USUARIO",
  LEAD = "LEAD",
  IA = "IA",
}
