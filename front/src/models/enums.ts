export enum WhatsAppProvider {
  BAILEYS = "BAILEYS",
  EVOLUTION = "EVOLUTION",
}

export enum InstanceStatus {
  ONLINE = "ONLINE",
  OFFLINE = "OFFLINE",
  CONNECTING = "CONNECTING",
  ERROR = "ERROR",
}

export enum Autor {
  USUARIO = "USUARIO",
  LEAD = "LEAD",
  IA = "IA",
}
